#
# def find_unique_value(numbers):
#     counts = {}
#     for num in numbers:
#         counts[num] = counts.get(num, 0) + 1
#
#     for num, count in counts.items():
#         if count == 1:
#             return num
#
# find_unique_value()
